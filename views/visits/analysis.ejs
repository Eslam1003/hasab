<!DOCTYPE html>
<html lang="ar">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>visit analaysis</title>
    <link rel="stylesheet" href="/stylesheets/analysis.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;300;400;500;600;700&family=Tajawal:wght@300;400;500;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="body">
    <nav class="navbar navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Home</a>
        <form action="/visits/analysis" method="post" class="d-flex">
          <label for="form">From</label>
          <input
            class="form-control me-2"
            type="date"
            placeholder="Search"
            aria-label="Search"
            name="from"
          />
          <label for="to">to</label>
          <input
            class="form-control me-2"
            type="date"
            placeholder="Search"
            aria-label="Search"
            name="to"
          />
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
      </div>
    </nav>

    <div>
      <% let agami=0 %> <% let one=0 %> <% let two=0 %> <% let three=0 %> <% let
      four=0 %> <% let swap=0 %> <% let amount=0 %> <% let del=0 %> <% let
      normal=0 %> <% for( let i = 0; i < visit.length; i++ ) { %> <%
      if(visit[i].aria==='0') { %> <% agami++ %> <% } %> <% if
      (visit[i].aria==='1') { %> <% one++ %> <% } %> <% if (visit[i].aria==='2')
      { %> <% two++ %> <% } %> <% if (visit[i].aria==='3') { %> <% three++ %> <%
      } %> <% if (visit[i].aria==='4') { %> <% four++ %> <% } %> <% if
      (visit[i].aria==='5') { %> <% swap++ %> <% } %> <% if
      (visit[i].stat==='red') { %> <% del++ %> <% } %> <% if
      (visit[i].stat==='white') { %> <% normal++ %> <% } %> <% amount=i+1 %> <%
      } %> <% let total=amount-del %>
    </div>
    <div class="card">
      <h5 class="card-title">اجمالي عدد الزيارات : <%= amount %></h5>
      <div class="card-body">
        <p>الزيارات الملغيه<%= del %></p>
        <p>الزيارات التي تمت : <%= total %></p>
        <p>العحمي : <%= agami %></p>
        <p>الاولي : <%= one %></p>
        <p>الثانيه : <%= two %></p>
        <p>الثالثه : <%= three %></p>
        <p>الرابعه : <%= four %></p>
        <p>المسحات : <%= swap %></p>
      </div>
    </div>
  </body>
</html>
